name: network_kit_lite
description: A Dart package inspired by Moya style for Flutter, providing a clean and maintainable way to consume APIs.
version: 0.1.0
homepage: https://github.com/yourusername/network_kit_lite

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.3.2
  shared_preferences: ^2.2.1
  sqflite: ^2.3.0
  path: ^1.8.3
  crypto: ^3.0.6
  easy_localization: ^3.0.7
  connectivity_plus: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.25.15
# flutter:
# assets:
# - assets/config/
# - assets/lang/
